name: Deploy CloudWear to AWS EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy application on EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            cd ~/cloudwear-networking
            git pull origin main
            sudo docker stop cloudwear-container || true
            sudo docker rm cloudwear-container || true
            sudo docker build -t cloudwear-app .
            sudo docker run -d \
              --name cloudwear-container \
              -p 3000:3000 \
              --env-file .env \
              cloudwear-app
            sudo docker ps
